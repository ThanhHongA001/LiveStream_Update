<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/rm_activity_home_fragment_container"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="10dp"
    android:orientation="vertical">

    <!-- FrameLayout chứa Fragment 1 -->
    <FrameLayout
        android:id="@+id/rm_activity_home_frame_01"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

    <!-- FrameLayout chứa Fragment 2 -->
    <FrameLayout
        android:id="@+id/rm_activity_home_frame_02"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

    <!-- FrameLayout chứa Fragment 3 -->
    <FrameLayout
        android:id="@+id/rm_activity_home_frame_03"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

    <!-- FrameLayout chứa Fragment 4 -->
    <FrameLayout
        android:id="@+id/rm_activity_home_frame_04"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

</LinearLayout>
